import 'package:flutter/material.dart';

class FollowUpType extends StatelessWidget {
  const FollowUpType({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
